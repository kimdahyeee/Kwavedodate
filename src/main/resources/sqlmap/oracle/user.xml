<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="user">
	<insert id="insertUser">
	insert into kwave_users 
	(userEmail, userPassword, userName, userRegdate, isSNS, userNation, phone, zipCode, address1, address2, city, region, country) 
	values (#{userEmail}, #{userPassword}, #{userName}, sysdate, 1 , null, null, null, null, null, null, null, null)
	</insert>
	
	<select id="selectUser" resultType="map">
		SELECT
			EMAIL as username,
			PASSWD as password,
			ENABLED as enabled,
			AUTHORITY as authority
		FROM USERTEST
		WHERE EMAIL = #{username}
	</select>
</mapper>